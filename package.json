{
  "scripts": {
    "build": "pnpm --recursive --filter=./packages/** build",
    "dev": "pnpm --recursive --parallel --filter=./packages/** dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "packages/*",
    "examples/*"
  ]
}
